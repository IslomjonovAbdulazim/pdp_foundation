part of 'imports.dart';

class LiveQuizResultPage extends StatelessWidget {
  const LiveQuizResultPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
